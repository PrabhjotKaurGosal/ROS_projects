<launch>

  <arg name="map_file" default="$(find hydra_one_navigation)/maps/cafe2d.yaml"/>

  <!-- Run the map server -->
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />

</launch>
